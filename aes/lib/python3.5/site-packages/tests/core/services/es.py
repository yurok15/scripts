from aes.services.elastic_search import ElasticSearchService


class ESTestService(ElasticSearchService):
    def __init__(self, name, controller, config, loop):
        super(ESTestService, self).__init__(
            name,
            controller,
            controller.settings['SERVICES'].get('elastic'),
            loop
        )

import os

import tests


def get_certs_folder():
    # find absolute path of ssl certs to be sure current working dir
    # would not affect loading
    path = os.path.dirname(tests.__file__)  # root of tests
    path = os.path.dirname(path)  # src
    return os.path.join(path, 'certs')  # certs

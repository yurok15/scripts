from aes.services.web.service import WebService

from aes.services.http import HttpService
from tests.core.services.forks import FakeForksService


class WebTestService(WebService):

    def requires(self, http: HttpService, forks: FakeForksService):
        super(WebTestService, self).requires(http, forks)
